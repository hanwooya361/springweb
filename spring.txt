[1] HTTP
[IP]
윈도우검색 --> CMD --> IPCONFIG --> ~~
IP: 네트워크 *컴퓨터* 식별번호 

[PORT]
윈도우검색 --> CMD --> NETSTAT --> ~~
PORT: *프로그램* 식별번호 (중복이 안됨, 프로그램 당 하나)

HTTP: 문서 교환 규칙, *무상태*(저장기능이 없음), 연결구조X
    - 서버가 먼저 요청X, 서버한테 요청하고 응답하고(단방향 통신)
    - 장점: 다수 요청 처리, 서버 부하 줄일 수 있는 성능 이점
SOKET: 상태가 있음, 연결구조
   -  양방향 통신
HTTPS: 인증과 암호화 한 웹 프로토콜

HTTP header
    - GET(조회), POST(저장), PUT(수정), DELETE(삭제)

HTTP Content-Type
    - JSON, HTML, FORM, MULTIPART

[2] WAS

Static pages: html, css, script

Dynamic page: jsp, react, servlet
    - servlet 클래스: http를 java에서 사용할수있게 구현한 클래스 (자바 기술), apache tomcat은 spring boot web에는 내장 됨, 8080
                      동적 실행, 웹컨테이너에 의해 관리, 스레드로 동작, mvc패턴의 controller, 상속받아 구현해야함

WAS(Web Application Server)
    - Web Server는 정적 컨텐츠(html.css.script) 제공 하지만 동적 컨텐츠(jsp, servlet)는 요청(Web Container)
    - WAS = Web Server + Web Container
    - Controller 같은 기능 수행

* 어노테이션:
1	@SpringBootApplication	Spring 실행할 때 spring에 필요한 기본세팅( 1.내장톰캣지원 2. 서블릿자동등록 )
2	@RestController	"Spring 에서는 서블릿을 상속이 아닌 @Controller 어노테이션으로 지원( HTTP )
+ RestController ( HTTP content-tpye 자동으로 application/json 세팅 )"
3	@PostMapping( "/board/save" )	만약에 HTTP 요청이 POST 이면 해당 메소드로 연결 + URL만들기(중복불가능)
4	@GetMapping( "/board/findall" )	만약에 HTTP 요청이 GET 이면 해당 메소드로 연결
5	@PutMapping( "/board/update" )	만약에 HTTP 요청이 PUT 이면 해당 메소드로 연결
6	@DeleteMapping( "/board/delete" )	만약에 HTTP 요청이 DELETE 이면 해당 메소드로 연결
*	HTTP 테스트는 Talend API / POSTMAN 에서 진행!

스프링 프로젝트내 
    src->main->java-> 자바파일(controller등) = 서블릿 (동작서버/웹컨테이너)
        src->main->java->example->day02->BoardController->@GetMapping("/board/findall")
        통신경로: http://127.0.0.1:8080/board/findall
        
    src->main->resouces->static(html/css/js등)-> 정적파일/웹서버
        src->main->resouces->static->day02->index.html파일생성
        통신경로는 static 이하부터 작성
        통신경로: http://127.0.0.1:8080/day02/index.html

어노테이션

리플렉션: 런타임환경에서의 클래스 구조를 읽고 객체 조작할 수 있는 기술
    1. new String(); [개발자가 직접]
    2. Class.forName(); [실행중에]

롬복:
    @NoArgsConstructor  // 매개변수 없는 생성자 (자동)생성
    @AllArgsConstructor // 전체 매개변수 있는 생성자 (자동)생성
    @RequiredArgsConstructor    // final 멤버변수의 생성자 (자동)생성
    @Getter @Setter // private 멤버변수의 getter/setter 메소드 (자동)생성
    @ToString       // 멤버변수 문자열 출력 메소드 (자동)생성
    @Data   // setter + getter + RequiredArgsConstructor + toString 묶음
    @EqualsAndHashCode  // 객체내 멤버변수의 값 모두 비교 메소드 (자동)생성
    @Builder    // 빌더 패턴 지원
    
IOC/DI
    @Autowired  // 의존성 주입
    @Component  // 싱글톤 대신에 @Component 이용해 IOC(자동 객체 관리) 규칙으로 스프링 컨테이너에 자동 빈(객체)등록

REST API
    @ResponseBody린? 자바의타입 --> HTTP content-type 변환해 동일하게 사용/직렬화
    - String: text/plain;
    - 그 외: application/json
    
    Map vs Dto
    - Map: 클래스 만들지 않고 자료 담기, 메소드 없음
    - Dto: 클래스를 만들고 그 안에 자료 담기, 메소드 있음
    
    @RestController // 3. 응답content-type을 application/json 자동 설정(@ResponseBody) + @Controller
    // 활용: VIEW(HTML) -> @Controller , JSON(값) -> @RestController

    @RequestParam란? 요청 content-type: form 또는 쿼리스트링의 매개변수 매핑/연결, 생략 가능

    @RequestMapping("/day03")   // 클래스내 메소드들의 공통url 정의

    요청Requset --------> 
                  HTTP
                <--------   응답Response
    * HTTP content-type: 전송하는 데이터의 타입 뜻
    쿼리스트링이란? URL?매개변수명=값&매개변수명=값 
        - GET/POST/PUT/DELETE 모두 사용 가능
    경로구분자이란? url/값1/값2
        - GET/POST/PUT/DELETE 모두 사용 가능
    1. @RequestParam: 쿼리스트링 or content-type:'apllication/x-www-form-urlencoded'(기본값)
    2. @ModelAttribute(생략가능): +DTO 매핑
    3. @PathVariable: 경로구분자
    4. @RequestBody: content-type:'apllication/json'
        - POST/PUT 메소드만 body(본문-경로상의값 노출이 없다) 지원한다.

JPA: 
    ORM: 객체와 테이블 중간에서 자동으로 매핑해주는 기술적 패러다임
        - JPA 핵심 매커니즘: 영속성
            - 영속성이란: 프로그램이 종료되어 메모리가 날아가도 데이터를 영구히 유지하는 성질
                - 1차 캐시: 한 트랜젝션 내에 동일한 pk로 조회 시 db 거치지 않고 메모리에서 반환
                - 더티 체킹: 엔티티 객체 값만 변경(setter)하면 update sql을 자동 전송(그래서 개발자가 update() 메소드를 별도 호출필요 x)

    Spring mvc패턴
        client -> controller -> service -> repository --entity--> db


********어노테이션 정리********
자바
    1. @Override: 상위 클래스/인터페이스의 메서드를 재정의함을 명시 및 검증
    2. @Retention: 어노테이션의 유지 범위(SOURCE, CLASS, RUNTIME) 지정
    3. @Target: 어노테이션이 적용될 대상(TYPE, METHOD, FIELD 등) 지정

롬복
    1. @NoArgsConstructor: 기본 생성자 자동 생성
    2. @AllArgsConstructor: 모든 필드를 포함하는 생성자 자동 생성
    3. @Getter: Getter 메서드 자동 생성
    4. @Setter: Setter 메서드 자동 생성
    5. @ToString: toString() 메서드 자동 생성
    6. @RequiredArgsConstructor: final 필드만 포함하는 생성자 자동 생성
    7. @Builder: 빌더 패턴 방식의 객체 생성 메서드 자동 생성

스프링
    1. @SpringBootApplication: 톰캣 자동 구성, 컴포넌트 스캔, 설정 기능을 포함한 시작점 지정
    2. @Component: 스프링 빈(Bean, 객체)으로 등록
    3. @Autowired: 스프링 빈(객체) 의존성 자동 주입

스프링 HTTP
    1. @ResponseBody: 반환 객체를 HTTP 응답 본문(JSON/XML/Text)으로 전송
    2. @RequestMapping: 클래스 또는 메서드 레벨에서 공통 URI 경로 및 HTTP 속성을 정의
    3. @GetMapping: HTTP GET 요청 전용 매핑
    4. @PostMapping: HTTP POST 요청 전용 매핑
    5. @PutMapping: HTTP PUT 요청 전용 매핑
    6. @DeleteMapping: HTTP DELETE 요청 전용 매핑
    7. @RequestParam: 쿼리 스트링 또는 폼 파라미터(application/x-www-form-urlencoded) 값 추출
    8. @ModelAttribute: 요청 파라미터를 객체(DTO) 필드에 저장
    9. @PathVariable: URI 경로 자체에 포함된 식별자 값 추출
    10. @RequestBody: HTTP body(JSON, XML 등, POST/PUT에서 주로 사용) 데이터를 Java 객체로 저장

스프링 MVC
    1. @Controller: Spring MVC 컨트롤러 등록 (프론트엔드파일 반환용)
    2. @RestController: RESTful 웹 서비스 컨트롤러 등록 (@Controller + @ResponseBody)
    3. @Service: 비즈니스 로직을 수행하는 서비스 계층 스프링 빈(객체) 등록
    4. @Repository: 데이터베이스 접근을 담당하는 영속성 계층 스프링 빈(객체) 등록 ( JpaRepository 인터페이스를 상속하여 구현해야 함)

스프링 JPA
    1. @Entity: 데이터베이스 테이블과 매핑될 JPA 관리 객체 지정
    2. @Table: 엔티티와 매핑할 데이터베이스 테이블 이름 및 세부 설정 지정
    3. @Id: 데이터베이스 테이블의 기본 키(Primary Key) 지정
    4. @Transactional: 트랜잭션 종료 시 엔티티 변경 감지를 통해 UPDATE 쿼리를 자동 실행 및 커밋 제어


1. 자바 
    @Override: 
    @Retention: 
    @Target: 

2. 롬복
    @NoArgsConstructor: 
    @AllArgsConstructor: 
    @Getter: Getter 
    @Setter: Setter 
    @ToString: 
    @RequiredArgsConstructor: 
    @Builder: 

3. 스프링
    @SpringBootApplication: 
    @Component: 
    @Autowired: 
    
4. 스프링 HTTP
    @ResponseBody: 
    @RequestMapping: 
    @GetMapping: 
    @PostMapping: 
    @PutMapping: 
    @DeleteMapping: 
    @RequestParam: 
    @ModelAttribute: 
    @PathVariable:
    @RequestBody: 

5. 스프링 MVC
    @Service: 
    @Repository: 
    @Controller: 
    @RestController: 

6. JPA
    @Entity:
    @Table(name="테이블명"):
    @Id: 
    @Transactional: 
